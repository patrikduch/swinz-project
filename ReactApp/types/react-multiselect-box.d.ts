export default class _default {
  static defaultProps: {
    addAllLabel: string;
    boxHeight: number;
    removeAllLabel: string;
    searchPlaceHolder: string;
    selectedLabel: string;
    valueArray: any[];
  };
  constructor(e: any);
  forceUpdate(callback: any): void;
  getFilteredOptions(): any;
  render(): any;
  setState(partialState: any, callback: any): void;
}
export default namespace _default {
  namespace propTypes {
    function addAllLabel(e: any, t: any, n: any, o: any, l: any, s: any): void;
    namespace addAllLabel {
      // Circular reference from index.default.propTypes.addAllLabel
      const isRequired: any;
    }
    function id(e: any, t: any, n: any, o: any, l: any, s: any): void;
    namespace id {
      // Circular reference from index.default.propTypes.id
      const isRequired: any;
    }
    function labelKey(e: any, t: any, n: any, o: any, l: any, s: any): void;
    namespace labelKey {
      // Circular reference from index.default.propTypes.labelKey
      const isRequired: any;
    }
    function onAdd(e: any, t: any, n: any, o: any, l: any, s: any): void;
    namespace onAdd {
      // Circular reference from index.default.propTypes.onAdd
      const isRequired: any;
    }
    function onRemove(e: any, t: any, n: any, o: any, l: any, s: any): void;
    namespace onRemove {
      // Circular reference from index.default.propTypes.onRemove
      const isRequired: any;
    }
    function onRemoveAll(e: any, t: any, n: any, o: any, l: any, s: any): void;
    namespace onRemoveAll {
      // Circular reference from index.default.propTypes.onRemoveAll
      const isRequired: any;
    }
    function onSelectAll(e: any, t: any, n: any, o: any, l: any, s: any): void;
    namespace onSelectAll {
      // Circular reference from index.default.propTypes.onSelectAll
      const isRequired: any;
    }
    function options(e: any, t: any, n: any, o: any, l: any, s: any): void;
    namespace options {
      // Circular reference from index.default.propTypes.options
      const isRequired: any;
    }
    function removeAllLabel(e: any, t: any, n: any, o: any, l: any, s: any): void;
    namespace removeAllLabel {
      // Circular reference from index.default.propTypes.removeAllLabel
      const isRequired: any;
    }
    function valueArray(e: any, t: any, n: any, o: any, l: any, s: any): void;
    namespace valueArray {
      // Circular reference from index.default.propTypes.valueArray
      const isRequired: any;
    }
    function valueKey(e: any, t: any, n: any, o: any, l: any, s: any): void;
    namespace valueKey {
      // Circular reference from index.default.propTypes.valueKey
      const isRequired: any;
    }
  }
}
